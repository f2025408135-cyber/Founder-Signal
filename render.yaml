services:
  - type: web
    name: founder-signal-api
    runtime: python
    buildCommand: pip install -r requirements-demo.txt
    startCommand: uvicorn scripts.demo_api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
