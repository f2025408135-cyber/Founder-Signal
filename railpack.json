{
  "$schema": "https://schema.railpack.com",
  "provider": "python",
  "steps": {
    "install": {
      "commands": ["pip install -r requirements-demo.txt"]
    }
  },
  "deploy": {
    "startCommand": "uvicorn scripts.demo_api:app --host 0.0.0.0 --port $PORT"
  }
}
